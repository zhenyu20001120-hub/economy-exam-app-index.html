# 中级经济师刷题应用 · 云端部署与使用指南（已上线 ✅）

> 应用已经帮你部署到 GitHub Pages 了，**直接打开网址就能用**，不用再自己操作。
> 下面是你自己的信息，方便你以后查看或重新部署。

---

## 🌐 你的在线网址（手机/电脑都能打开）
**https://zhenyu20001120-hub.github.io/economy-exam-app-index.html/**

把这个链接发到手机浏览器，或微信/备忘录里存着，随时点开刷题。

> 备用链接（CloudStudio 发的，也能用）：
> https://c1d2962012644fd2950ddd6930d3261e.app.workbuddy.link

---

## 📦 仓库信息（你可能用得上）
- 仓库名：**economy-exam-app-index.html**（已设为**公开 Public**）
- 地址：https://github.com/zhenyu20001120-hub/economy-exam-app-index.html
- 分支：**main**
- 里面的文件：`index.html`（应用本体）、`.nojekyll`（让 GitHub 原样显示，不改动你的代码）

---

## 📱 怎么用
打开网址后，底部有 5 个按钮：
- **首页**：看总进度、选练习模式、"继续上次练习"
- **章节**：按考纲 11 章逐章刷
- **知识**：先看整章思维导图+讲解，再开刷（"以练"设计）
- **错题**：只刷答错的题
- **模考**：全真 100 题、限时 90 分钟
- 首页右上角 **⚙️ 云同步与设置**：开启跨设备同步（见下）

> 进度默认存在你**打开它的那个浏览器**里。只在一台设备刷（比如只手机），不用任何设置，自动记住。

---

## 🔄 跨设备同步（电脑↔手机共享同一份进度和错题）
应用本身带云同步，需要你填一个 GitHub 令牌（只存在你浏览器里，不经过任何人）：
1. GitHub 右上角头像 → **设置** → 最底 **开发者设置** → **个人访问令牌** → **细粒度令牌** → **生成新令牌**。
2. 填表：名称随便（如 `ee-sync`）；过期时间 **90 天**；资源所有者选**你自己**；仓库访问 **仅选定的存储库** → 选 `economy-exam-app-index.html`；权限 → 仓库权限 → **内容** 选 **读写**。
3. 点生成 → 立刻复制那串 `github_pat_...`（只显示一次！存手机备忘录）。
4. 打开网址 → 首页 **⚙️ 云同步与设置** → 开关打开 → 填：
   - 仓库：`zhenyu20001120-hub/economy-exam-app-index.html`
   - 令牌：粘 `github_pat_...`
   - 数据分支：`ee-sync`（首次自动建）
5. 点**保存设置**。另一台设备打开同一网址、填一样的内容、点"从云端恢复"就同步了。

> 你之前说"公开也无所谓"，所以直接复用这个公开仓库的 `ee-sync` 分支存进度，省事。若以后想更私密，再建一个私有小仓库 `ee-exam-data` 专门存进度即可。

---

## 🛠 以后要更新应用（改题/加功能后怎么重新发）
1. 我在本地改好 `economy-exam-app/index.html` 后，用 GitHub 命令行（`gh`）上传：
   ```
   gh auth login --web          # 首次需登录一次（设备码，不用把令牌给我）
   # 把 index.html 转成 base64 后 PUT 上传到仓库（具体命令我执行时给你）
   ```
2. 上传后等 **1–3 分钟**，刷新网址就是新版。

> 这一步通常由我帮你操作，你不用自己弄。

---

## ❓ 常见问题
- **网址打不开 / 显示旧内容**：等 1–3 分钟再刷；强刷 `Ctrl+F5`（Mac `Cmd+Shift+R`）。
- **想改题库/加功能**：直接跟我说，我改完重新部署。
- **令牌 90 天过期**：回「跨设备同步」第 1–4 步重新生成一个，应用里重新填。
